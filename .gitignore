# Local dependencies and caches
node_modules/
__pycache__/
*.py[cod]
.pytest_cache/
.DS_Store
Thumbs.db
*.log

# Credentials and machine-local settings
.env
.env.*
!.env.example
*.pem
*.key
.vscode/
.idea/

# Client and production records belong outside the code repository
/uploads/
/orders/
/pedidos/
/exports/
/backups/
/references/
*-pedido-completo.json
*-pedido.json

# Generated cloud library snapshot (embedded into dist/index.html at build)
photostudio/cloud/snapshot.json

# Local packaging output; dist/index.html is intentionally versioned
*.zip
*.bundle
